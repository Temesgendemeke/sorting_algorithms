#include "sort.h"
/**
 *  selection_sort - sorts 
 * @array: to be sorted
 * @size: length of array
 */
void selection_sort(int *array, size_t size)
{
    int i,j;
    int min;

    for(i = 0; i < size; i++)
    {
       for(j)

    }
}